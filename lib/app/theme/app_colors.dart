import 'package:flutter/material.dart';

class AppColors {
  // Deep charcoal/black base for a calm, premium feel
  static const Color background = Color(0xFF0A0D14);
  static const Color surface = Color(0xFF131722);
  static const Color surfaceVariant = Color(0xFF1E2333);
  
  // Muted emerald secondary (Islamic green, but modern and subtle)
  static const Color emerald = Color(0xFF10B981);

  // Soft gold accent for active controls (play buttons, active surah)
  static const Color goldAccent = Color(0xFFD4AF37);

  // Text Colors
  static const Color textPrimary = Color(0xFFF8FAFC);   // Pure white-ish
  static const Color textSecondary = Color(0xFF94A3B8); // Muted grey for subtitles
}